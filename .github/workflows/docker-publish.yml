name: Docker publish

# Builds the statewave-admin image and pushes it to:
#   - Docker Hub: statewavedev/statewave-admin
#   - GHCR:       ghcr.io/smaramwbc/statewave-admin
#
# Multi-arch (linux/amd64, linux/arm64), with build provenance + SBOM, signed
# via Sigstore through actions/attest-build-provenance.
#
# Triggers:
#   - push to master         → :latest, :sha-<short>
#   - push of v* tag         → :X.Y.Z, :X.Y, :X (semver)
#   - workflow_dispatch      → manual rerun
#
# Required repo wiring (already configured at the repo level):
#   - vars.DOCKERHUB_USERNAME = statewavedev
#   - secrets.DOCKERHUB_TOKEN = a Docker Hub access token with R/W on the
#     statewavedev namespace

on:
  push:
    branches: [master]
    tags: ["v*"]
  workflow_dispatch:

concurrency:
  group: docker-publish-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read
  packages: write
  id-token: write
  attestations: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            statewavedev/statewave-admin
            ghcr.io/smaramwbc/statewave-admin
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=sha,format=short
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}
          labels: |
            org.opencontainers.image.title=Statewave Admin
            org.opencontainers.image.description=Statewave admin console — operator UI for Statewave servers
            org.opencontainers.image.vendor=Statewave
            org.opencontainers.image.licenses=AGPL-3.0
            org.opencontainers.image.source=https://github.com/smaramwbc/statewave-admin
            org.opencontainers.image.documentation=https://statewave.ai

      - id: push
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          provenance: mode=max
          sbom: true

      - name: Attest build provenance — Docker Hub
        uses: actions/attest-build-provenance@v2
        with:
          subject-name: index.docker.io/statewavedev/statewave-admin
          subject-digest: ${{ steps.push.outputs.digest }}
          push-to-registry: true

      - name: Attest build provenance — GHCR
        uses: actions/attest-build-provenance@v2
        with:
          subject-name: ghcr.io/smaramwbc/statewave-admin
          subject-digest: ${{ steps.push.outputs.digest }}
          push-to-registry: true

      # Sync DOCKER.md to the Docker Hub repo description on every default-branch
      # build. Idempotent — re-running with unchanged content is a no-op. Only
      # runs on push events so workflow_dispatch / tag builds don't churn it.
      - name: Update Docker Hub description
        if: github.event_name == 'push' && github.ref == 'refs/heads/master'
        uses: peter-evans/dockerhub-description@v4
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
          repository: statewavedev/statewave-admin
          short-description: "Operator console for Statewave servers"
          readme-filepath: ./DOCKER.md

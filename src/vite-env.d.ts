/// <reference types="vite/client" />

// Injected by Vite `define` (see vite.config.ts) — the package.json version,
// so the UI version label tracks the real release instead of a hardcoded string.
declare const __ADMIN_VERSION__: string
